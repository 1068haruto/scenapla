<p>
  <% if withdrawal == 0 %>
    プラス収支の取り崩し期間：なし
  <% else %>
    プラス収支の取り崩し期間：<%= withdrawal %>ヶ月目まで
  <% end %>
  <br>
  <% if shortage == 0 %>
    現在の不足額：なし
  <% else %>
    現在の不足額：<%= shortage %>万円（年間）
  <% end %>
</p>